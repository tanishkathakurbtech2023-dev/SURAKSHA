# SURAKSHA Frontend (Current Prototype)

This folder now contains a lightweight HTML/CSS/JS prototype:
- `index.html`: dashboard with forms for risk scoring and transaction processing
- `app.js`: API integration with backend endpoints
- `styles.css`: basic responsive styling

The backend serves this UI at `/` and static assets at `/frontend/*`.
